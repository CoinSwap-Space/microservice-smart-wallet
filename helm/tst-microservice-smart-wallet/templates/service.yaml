apiVersion: v1
kind: Service
metadata:
  name: {{ include "tst-microservice-smart-wallet.fullname" . }}
  labels:
    {{- include "tst-microservice-smart-wallet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tst-microservice-smart-wallet.selectorLabels" . | nindent 4 }}